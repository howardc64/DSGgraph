DSGgraph.txt is source commands to Claude to generate DSGgraph.py
  including existing py and html as referece reduce AI randomness (different graph, front style etc)

CSV files are reference VCDS generated logs with VCDSparam
VCDS will append each run to the last one (same day?) need to hand partition these into individual runs to give to python script
html files are generated by the python script for the corresponding CSV file
